# Build fixture for the builder-ghcr self-repo test case.
# ponytail: scratch keeps the build dependency-free and near-instant; swap in a
# real base only if a test ever needs to run something inside the image.
FROM scratch
COPY Dockerfile /Dockerfile
